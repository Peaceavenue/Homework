export {}
const a: string = "доступ";
const b: string = "морпех";
const c: string = 'наледь';
const d: string = "попрек";
const e: string = "рубило";
const lengthWords: number = (a.length + b.length + c.length + d.length + e.length);
console.log(lengthWords);